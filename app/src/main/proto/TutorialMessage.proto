syntax = "proto3";

package com.example.grpcdemo.actions;
option java_multiple_files = false;

import "actions/GetScore.proto";

service TutorialActionService {
    rpc GetScore (actions.GetScoreResponse) returns (actions.GetScoreResponse);
}